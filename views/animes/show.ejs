<body>
    <div class="container">
        <%- include('../partials/header.ejs')%>
    </div>
    <h2 class="header"><%= anime.title %></h2>
    <img width="200" height="300" src="<%= anime.posterImagePath %>">
    <div>Studio: <%= anime.studio.name %></div> 
    <div>Airing Date: <%= anime.dateAired.toDateString() %></div>
    <div>Episodes: <%= anime.episodeCount %></div>
    <div>Description: <%= anime.description %></div>
    <a class="buttons primary-button" href="/animes/<%= anime.id %>/edit">Edit</a>
    <%- include('../partials/delete', { url: `/animes/${anime.id}` }) %>
    <a class="buttons primary-button" href="/studios/<%= anime.studio.id %>">View Studio</a>
</body>